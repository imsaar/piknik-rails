<!DOCTYPE html>
<html>
  <head>
    <title>Piknik</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
      /* Piknik App Custom Styles */
      body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f8f9fa;
        color: #333;
      }
      .piknik-home-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
      }
      .piknik-hero {
        text-align: center;
        padding: 3rem 1rem;
        background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
        color: white;
        border-radius: 12px;
        margin-bottom: 2rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      }
      .piknik-hero h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
      }
      .tagline {
        font-size: 1.5rem;
        opacity: 0.9;
      }
      .piknik-cta-container {
        display: flex;
        gap: 2rem;
        margin-bottom: 3rem;
      }
      .piknik-cta-card {
        flex: 1;
        background: white;
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        transition: transform 0.3s ease;
      }
      .piknik-cta-card:hover {
        transform: translateY(-5px);
      }
      .piknik-cta-button {
        display: inline-block;
        padding: 1rem 2rem;
        margin-top: 1rem;
        border-radius: 30px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
      }
      .piknik-cta-button.create {
        background-color: #2575fc;
        color: white;
      }
      .piknik-cta-button.join {
        background-color: #6a11cb;
        color: white;
      }
      .piknik-cta-button:hover {
        transform: scale(1.05);
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      }
      .piknik-features {
        text-align: center;
        padding: 2rem 0;
      }
      .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
      }
      .feature-item {
        background: white;
        padding: 1.5rem;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
      }
      .feature-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
      }
      /* Responsive adjustments */
      @media (max-width: 768px) {
        .piknik-cta-container {
          flex-direction: column;
        }
        .piknik-hero h1 {
          font-size: 2.5rem;
        }
        .tagline {
          font-size: 1.2rem;
        }
      }
    </style>
  </head>

  <body>
    <header class="piknik-header">
      <div class="piknik-home-container">
        <nav style="display: flex; justify-content: space-between; padding: 1rem 0;">
          <a href="/" style="text-decoration: none; color: #333; font-weight: 700; font-size: 1.5rem;">Piknik</a>
          <div class="nav-links">
            <a href="/" style="margin-right: 1rem; text-decoration: none; color: #333;">Home</a>
            <a href="/events/new" style="margin-right: 1rem; text-decoration: none; color: #333;">Create</a>
            <a href="/join" style="text-decoration: none; color: #333;">Join</a>
          </div>
        </nav>
      </div>
    </header>
    
    <%= yield %>
    
    <footer style="background-color: #f1f3f5; padding: 2rem 0; margin-top: 3rem; text-align: center;">
      <div class="piknik-home-container">
        <p>© <%= Date.today.year %> Piknik App - Make outdoor gatherings better</p>
      </div>
    </footer>
  </body>
</html>
